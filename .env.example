# Optional override for feed OPML path
# NEWSCLI_OPML_PATH=./feeds.opml

# Optional cache directory path (default: $XDG_CACHE_HOME/news or ~/.cache/news)
# NEWSCLI_CACHE_DIR=./tmp/newscli-cache

# Cache freshness in minutes
# NEWSCLI_CACHE_TTL_MINUTES=30
